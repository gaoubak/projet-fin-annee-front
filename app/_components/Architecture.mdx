# Architecture

le dossier porte le nom du composant avec dedans les fichiers suivants:

- `nom-du-composant.component.tsx` : le template tsx du composant
- `nom-du-composant.styles.scss` : le style du composant
- `nom-du-composant.component.spec.tsx` : les tests du composant
